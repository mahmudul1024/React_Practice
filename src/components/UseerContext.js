import React from 'react'
export const UserContext =React.createContext();

//will provide provider or,consumer

// step 1 :create context
//step 2: wrap childs with context provider
//step3 :State access useContext hook
