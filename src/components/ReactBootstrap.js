import React from 'react'
import Button from 'react-bootstrap/Button';
import Card from 'react-bootstrap/Card';


const ReactBootstrap = () => {
  return (
    <Card style={{ width: '20rem' }} >
    <Card.Body>
    <h1 className="headingStyle">ReactBootstrap</h1>
     <Card.Text className='head'>without any file name to unstage all due changes. 
     This can come in handy when there are too many files to be listed one by </Card.Text>
     <Button>Learn more</Button>
    </Card.Body>
     </Card>
  )
}

export default ReactBootstrap
